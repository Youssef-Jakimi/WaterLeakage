numpy>=1.24
pandas>=2.0
networkx>=3.1
torch>=2.1
matplotlib>=3.8
jinja2>=3.1
scikit-learn>=1.4

# Optional, enabled if you want PyG Temporal-native data containers and models.
torch-geometric-temporal>=0.56.0
torch-geometric>=2.4.0

# Optional hydraulic-engine backends. The project runs without them thanks to fallbacks.
wntr>=1.2.0

# Install any local EPANET wrapper separately if needed.
